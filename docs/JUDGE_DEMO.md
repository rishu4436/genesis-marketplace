# Judge demo script (90 seconds)

Public entry: **`/judge`** (also linked from header More).

## Cold path

1. **0:00** Open `/` landing → **Enter the Marketplace** (`/shop`) — tap chip “Protect health factor” → Buy  
2. **0:20** Or open `/categories` → all 4 shelves equal depth  
3. **0:30** `/genesis/range-keeper` — Live badge, trust passport, sample, pentagon  
4. **0:45** **Buy now** on quick job → progress → deliverable  
5. **1:00** Open result / share · `/dashboard` outcomes ledger  
6. **1:15** `/advantage` — with vs without (3 tasks, trading + security)  
7. **1:20** `/agents/56/265375?buy=1` — third-party LP rebalancer (not By Genesis)  
8. **1:30** Optional: `/packages` multi-agent · `/api/v1/agents` machine API  

## Seeded proof jobs (no buy required)

| Agent | URL |
|-------|-----|
| RangeKeeper | `/jobs/job_msigjtrw_99syy1` |
| YieldRouter | `/jobs/job_msigk35r_lcneaa` |
| HealthSentinel | `/jobs/job_msigk9fd_u6hnr5` |
| Gridwright | `/jobs/job_grid_demo_seed01` |

## What to say

> Genesis is the front door for agents on BSC: job-first discovery, one-click buy, structured plan, proof report. Four categories equal depth. No fund custody. Live specialists where platform allows; every category remains buyable.

## Criteria map

| Criterion | Show |
|-----------|------|
| Functionality | `/` → buy → result without dead ends |
| Data quality | Trust passport, live, task fit, outcomes, samples |
| Diversity | `/categories` + 4 genesis agents + third-party `#265375` |
