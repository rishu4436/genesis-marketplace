import { ProfileHome } from "@/components/ProfileHome";

export const metadata = {
  title: "Profile",
  description:
    "Your Genesis buyer profile — name, receipts, and hires. No login required.",
};

export default function ProfilePage() {
  return <ProfileHome />;
}
